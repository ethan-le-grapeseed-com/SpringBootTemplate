package com.example.core.application.usecase;

/**
 * Marker interface for all use cases (commands and queries).
 * 
 * @param <TResponse> The response type
 */
public interface UseCase<TResponse> {
}
